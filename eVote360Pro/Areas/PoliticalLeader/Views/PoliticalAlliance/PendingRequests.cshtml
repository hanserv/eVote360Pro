@model IEnumerable<PoliticalAllianceViewModel>
@{
	ViewData["Title"] = "Political Leader - Pending Requests";
	var showButtons = ViewBag.HasActiveElection ? false : true;
}

<div class="container-fluid mb-5">
	<div class="d-flex justify-content-between gap-2">
		<a asp-area="PoliticalLeader" asp-controller="PoliticalAlliance" asp-action="Index" class="btn btn-outline-dark fw-semibold"><i class="bi bi-arrow-left"></i> Go back</a>
	</div>

	@if (!showButtons)
	{
		<div class="card bg-warning mt-2 p-2">
			<h5 class="fs-5 text-center text-light fw-normal m-0"><i class="bi bi-info-circle-fill"></i> Political alliances cannot be modified while there is an active election.</h5>
		</div>
	}

	<div class="card shadow-sm border-0 mt-2">
		<div class="card-header bg-dark text-white">
			<h2 class="fs-4 m-0"><i class="bi bi-hourglass"></i> Pending Requests</h2>
		</div>

		<div class="card-body bg-white p-0">
			<div class="table-responsive">
				<table class="table table-bordered m-0 table-hover">
					<thead>
						<tr>
							<th>Requester party</th>
							<th>Received date</th>
							<th>Status</th>
							<th>Actions</th>
						</tr>
					</thead>
					<tbody>
						@if (Model is not null)
						{
							foreach (var item in Model)
							{
								<tr>
									<td>@item.RequesterParty.Name (@item.RequesterParty.Acronym)</td>
									<td>@item.RequestDate</td>
									<td>Waiting for response</td>
									<td>
										<div class="d-flex gap-2">
											<a asp-area="PoliticalLeader" asp-controller="PoliticalAlliance" asp-action="Accept" asp-route-id="@item.Id" class="@(showButtons ? "" : "pe-none")">
												<i class="bi bi-check-circle-fill text-success fs-5"></i>
											</a>
											<a asp-area="PoliticalLeader" asp-controller="PoliticalAlliance" asp-action="Reject" asp-route-id="@item.Id" class="@(showButtons ? "" : "pe-none")">
												<i class="bi bi-x-circle-fill text-danger fs-5"></i>
											</a>
										</div>
									</td>
								</tr>
							}
						}
					</tbody>
				</table>
			</div>
		</div>
	</div>
</div>
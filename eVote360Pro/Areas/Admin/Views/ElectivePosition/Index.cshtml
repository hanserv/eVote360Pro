@model IEnumerable<ElectivePositionViewModel>
@{
	ViewData["Title"] = "Admin - Elective Positions";
	var showButtons = ViewBag.HasActiveElection ? false : true;
}

<div class="container-fluid mb-5">
	<div class="d-flex justify-content-between gap-2">
		<a asp-area="Admin" asp-controller="Home" asp-action="Index" class="btn btn-outline-dark fw-semibold"><i class="bi bi-arrow-left"></i> Go back</a>
		<a asp-area="Admin" asp-controller="ElectivePosition" asp-action="Create" class="btn btn-outline-dark fw-semibold px-5 @(showButtons ? "" : "disabled")"><i class="bi bi-plus-lg"></i> Create elective position</a>
	</div>

	@* <div class="col-12 col-sm-4 col-md-4 col-xl-3 col-md-2 my-2">
		<a asp-area="Admin" asp-controller="ElectivePosition" asp-action="Create" class="btn btn-outline-dark fw-semibold w-100 @(showButtons ? "" : "disabled")"><i class="bi bi-plus-lg"></i> Create elective position</a>
	</div> *@

	@if (!showButtons)
	{
		<div class="card bg-warning mt-2 p-2">
			<h5 class="fs-5 text-center text-light fw-normal m-0"><i class="bi bi-info-circle-fill"></i> Elective positions cannot be changed while an election is active.</h5>
		</div>
	}

	<div class="card shadow-sm border-0 mt-2">
		<div class="card-header bg-dark text-white">
			<h2 class="fs-4 m-0"><i class="bi bi-person-rolodex"></i> Elective Positions</h2>
		</div>

		<div class="card-body bg-white p-0">
			<div class="table-responsive">
				<table class="table table-bordered m-0 table-hover">
					<thead>
						<tr>
							<th>Name</th>
							<th>Description</th>
							<th>Status</th>
							<th>Actions</th>
						</tr>
					</thead>
					<tbody>
						@if (Model is not null)
						{
							foreach (var item in Model)
							{
								<tr>
									<td>@item.Name</td>
									<td>@item.Description</td>
									<td>@(item.IsActive ? "Active" : "Inactive")</td>
									<td>
										<div class="d-flex gap-2">
											<a asp-area="Admin" asp-controller="ElectivePosition" asp-action="Edit" asp-route-id="@item.Id" class="@(showButtons ? "" : "pe-none")">
												<i class="bi bi-pencil-square text-warning fs-5"></i>
											</a>
											@if (item.IsActive)
											{
												<a asp-area="Admin" asp-controller="ElectivePosition" asp-action="ChangeStatus" asp-route-id="@item.Id" asp-route-active="false" class="btn btn-sm btn-outline-secondary @(showButtons ? "" : "disabled")">
													<span>Desactivate</span>
												</a>
											}
											else
											{
												<a asp-area="Admin" asp-controller="ElectivePosition" asp-action="ChangeStatus" asp-route-id="@item.Id" asp-route-active="true" class="btn btn-sm btn-outline-info @(showButtons ? "" : "disabled")">
													<span>Activate</span>
												</a>
											}
										</div>
									</td>
								</tr>
							}
						}
					</tbody>
				</table>
			</div>
		</div>
	</div>
</div>